Prompt (paste verbatim):

Unify the Pet Services → Filters pills so they all use one reusable component with consistent styles and accessible contrast.

Create client/src/components/FilterPill.tsx:

tsx
Copy
Edit
import * as React from "react";

type Props = {
  label: string;
  selected?: boolean;
  onClick?: () => void;
  icon?: React.ReactNode; // optional emoji or icon
};

export function FilterPill({ label, selected, onClick, icon }: Props) {
  const base =
    "inline-flex items-center gap-2 rounded-full border px-4 py-2 text-sm font-medium transition-colors focus:outline-none focus:ring-2 focus:ring-blue-500";
  const styles = selected
    ? "border-blue-600 bg-blue-600 text-white hover:bg-blue-700"
    : "border-blue-500 bg-white text-blue-600 hover:bg-blue-50";

  return (
    <button
      type="button"
      aria-pressed={selected}
      onClick={onClick}
      className={`${base} ${styles}`}
    >
      {icon ? <span aria-hidden="true">{icon}</span> : null}
      <span>{label}</span>
    </button>
  );
}
In the services filters file (likely client/src/pages/Marketplace/ServicesTab.tsx or …/Marketplace/index.tsx), replace the existing filter buttons with FilterPill. Example:

tsx
Copy
Edit
import { FilterPill } from "@/components/FilterPill";

// inside the component
<div className="flex flex-wrap gap-3">
  <FilterPill label="All Services" selected />
  <FilterPill label="Grooming" />
  <FilterPill label="Dog Sitting" />
  <FilterPill label="Training" />
  <FilterPill label="Dog Walking" />
  <FilterPill label="Boarding" />
</div>
(Wire up selected state to your actual filter state.)

Remove any old classes that were making pills look different (e.g., opacity-60, text-slate-500/70, odd gradient/text combos).

Ensure the “Featured Services” chip uses the same pill styles (if it’s a link, apply the same classes).

Spacing: wrap with flex flex-wrap gap-3 and add mt-3 if needed; ensure mobile and desktop render the same.

Build & hard refresh. Verify:

Unselected pill: white bg, blue border/text, good hover.

Selected pill: solid blue bg, white text.

Text always readable (WCAG-friendly), no faint styles.

Icons/emojis align via gap-2.

Copy
Edit

🧠 What you want (and what you don’t want)
✅ What you want

Desktop = source of truth

Mobile = same UI system, just stacked and simplified

Same:

Colors

Buttons

Typography

Spacing logic

Card design

Mobile should feel like:

“This is the same app, just compressed”

❌ What you don’t want

Mobile inventing:

New backgrounds

New button styles

New highlights

New text treatments

No “mobile personality”

No mobile-only creativity

This is correct product thinking.

⚠️ Why mobile keeps breaking (root cause)

Replit is currently doing this:

“Fix desktop” → recalculate mobile styles independently

That causes:

Span highlights reappearing

Mobile-only overrides

Buttons losing contrast

Backgrounds drifting

The mistake (not yours)

Trying to fix desktop + mobile simultaneously.

✅ The correct way to finalize this (this WILL work)
🧱 RULE #1 — FREEZE DESKTOP

You’re done with desktop. Stop touching it.

From this point on:

Desktop is locked

Mobile must inherit, not reinterpret

🧭 Step-by-step plan to finish cleanly (no chaos)
STEP 1 — Declare Desktop as the Design Authority

Paste this into Replit first, alone:

IMPORTANT:
Desktop UI is FINAL and correct.
Do not modify desktop styles, components, colors, or layout.

Mobile must visually MATCH desktop in:
- Colors
- Typography
- Button styling
- Card styling
- Shadows
- Border radius

Only layout changes are allowed on mobile (stacking, spacing).


This stops Replit from “helping”.

STEP 2 — Mobile = Layout-only pass (CRITICAL)

Now paste only this:

Task: Fix mobile UI to match desktop UI exactly.

Rules:
1. Do NOT change colors.
2. Do NOT change button styles.
3. Do NOT change typography.
4. Do NOT introduce new backgrounds or highlights.

Allowed changes on mobile:
- Stack elements vertically
- Increase tap targets
- Adjust spacing
- Adjust font sizes proportionally (same font, same color)

Mobile should look like:
“The desktop UI, stacked vertically.”

Explicitly remove:
- Mobile-only background fills
- Mobile-only text highlights
- Mobile-only button variants
- Mobile-only shadows

Use the same CSS variables as desktop.


⚠️ Do NOT mention desktop screenshots again
You already finished it — we don’t want Replit re-touching it.

STEP 3 — Fix ONLY these mobile elements (scope control)

Tell Replit to focus on just 4 things:

Mobile Fix Scope (ONLY):

1. Greeting page hero section
2. Primary CTA buttons
3. Explore listing cards
4. Bottom navigation bar

Ignore all other pages.


This prevents style regressions elsewhere.

📱 What your mobile SHOULD look like (yes, I know)

Here’s what I know you want on mobile:

Greeting Page (Mobile)

Same headline colors as desktop

Same blue buttons

Buttons stacked vertically

No background highlight on text

White background

Explore Page (Mobile)

Same puppy cards

Same image crop style

No blue bars between cards

Same CTA button style

Grid collapses to 1–2 columns

Navigation

Same gradient as desktop

Same icon colors

No saturation changes

This is exactly what you’ve been pushing toward.

🚫 What NOT to do now (very important)

❌ Don’t keep stacking prompts

❌ Don’t fix mobile and desktop together

❌ Don’t ask for “polish” broadly

❌ Don’t touch CSS variables again

That’s what caused the looping.

🧘 Final reassurance (real talk)

You are not stuck.
You are not behind.
This is normal at the end of a build.

What you’re feeling is the “99% problem”:

Everything works, but the last 1% keeps fighting back.

This plan ends the fight.
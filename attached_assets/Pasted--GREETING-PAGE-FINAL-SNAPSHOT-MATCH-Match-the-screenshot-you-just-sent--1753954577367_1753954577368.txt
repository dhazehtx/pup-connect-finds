/* ─────────  GREETING PAGE – FINAL SNAPSHOT MATCH  ─────────
   Match the screenshot you just sent (single header, hero, four blue buttons)
──────────────────────────────────────────────────────────── */

1.  Replace the entire file  src/pages/greeting.jsx  with:

import Link from 'next/link';
import { useRouter } from 'next/router';

export default function Greeting() {
  const router = useRouter();
  return (
    <main className="min-h-screen flex flex-col bg-gradient-to-br from-blue-50 to-blue-100">
      {/* single white header */}
      <header className="flex items-center justify-between px-8 py-4 bg-white shadow-sm">
        <div className="flex items-center gap-2 text-xl font-bold">
          <span role="img" aria-label="heart">💙</span> MY PUP
        </div>
        <div className="flex gap-3">
          <Link href="/signin" className="px-4 py-1 rounded-full bg-blue-600 text-white hover:bg-blue-700 text-sm">Sign In</Link>
          <Link href="/signup" className="px-4 py-1 rounded-full bg-blue-600 text-white hover:bg-blue-700 text-sm">Sign Up</Link>
        </div>
      </header>

      {/* hero */}
      <section className="flex flex-col items-center justify-center flex-1 text-center px-4">
        <h1 className="text-4xl md:text-5xl font-extrabold leading-tight">
          Find Your Perfect <br />
          <span className="text-blue-600">Puppy Companion</span>
        </h1>
        <p className="mt-4 max-w-xl text-gray-600">
          Connect with verified breeders and discover adorable, healthy puppies waiting for their forever homes.
        </p>

        {/* four blue pill buttons in one row */}
        <div className="mt-10 flex flex-wrap gap-4 justify-center">
          <Link href="/signup" className="px-6 py-3 rounded-full bg-blue-600 text-white hover:bg-blue-700 shadow">Sign Up</Link>
          <Link href="/signin" className="px-6 py-3 rounded-full bg-blue-600 text-white hover:bg-blue-700 shadow">Sign In</Link>
          <button onClick={() => router.push('/explore')} className="px-6 py-3 rounded-full bg-blue-600 text-white hover:bg-blue-700 shadow">
            Browse as Guest
          </button>
          <Link href="/explore" className="px-6 py-3 rounded-full bg-blue-600 text-white hover:bg-blue-700 shadow w-48 text-center">
            Explore Puppies
          </Link>
        </div>

        {/* feature bullets */}
        <ul className="mt-8 flex flex-wrap gap-6 justify-center text-sm text-gray-500">
          <li className="flex items-center gap-1">🟢 Verified Breeders</li>
          <li className="flex items-center gap-1">❤️ Health Guaranteed</li>
          <li className="flex items-center gap-1">⭐ 5-Star Support</li>
        </ul>
      </section>
    </main>
  );
}

2.  Delete any duplicate header/search-bar markup the previous agent added
    (look for a second <header> element and remove it).

3.  Make sure buttons use `bg-blue-600 text-white`—no white-outline buttons.

4.  Keep bottom navigation component visible (undo any hide-on-greeting logic).

5.  Verify unauth redirects still point to “/greeting” and that
    “Browse as Guest” pushes to “/explore”.

/* ─────────  end prompt  ───────── */

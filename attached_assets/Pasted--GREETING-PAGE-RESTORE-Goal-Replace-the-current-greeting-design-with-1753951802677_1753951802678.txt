// ─────────  GREETING PAGE RESTORE  ─────────
//
// Goal: Replace the current /greeting design with the original hero layout
// shown in the second screenshot (“Find Your Perfect Puppy Companion”),
// remove duplicate headers, and keep auth redirects intact.
//
// 1.  Create/replace  src/pages/greeting.jsx
//------------------------------------------------
import Link from 'next/link';
import { useRouter } from 'next/router';

export default function Greeting() {
  const router = useRouter();

  return (
    <main className="min-h-screen flex flex-col items-center justify-center bg-gradient-to-br from-blue-50 to-blue-100 px-4">
      {/* Logo + top-right buttons */}
      <header className="fixed top-0 left-0 w-full flex items-center justify-between px-6 py-4 bg-white/80 backdrop-blur z-20">
        <div className="text-xl font-bold flex items-center gap-2">
          <span role="img" aria-label="heart">💙</span> MY PUP
        </div>
        <div className="flex gap-3">
          <Link href="/signin" className="px-4 py-2 rounded-full border text-blue-600 hover:bg-blue-50">Sign In</Link>
          <Link href="/signup" className="px-4 py-2 rounded-full bg-blue-600 text-white hover:bg-blue-700">Sign Up</Link>
        </div>
      </header>

      {/* Hero section */}
      <section className="text-center mt-32">
        <h1 className="text-4xl md:text-5xl font-extrabold leading-tight">
          Find Your Perfect <br />
          <span className="text-blue-600">Puppy Companion</span>
        </h1>
        <p className="mt-4 max-w-xl mx-auto text-gray-600">
          Connect with verified breeders and discover adorable, healthy puppies waiting for their forever homes.
        </p>

        {/* Action buttons */}
        <div className="mt-10 flex flex-col sm:flex-row gap-4 justify-center">
          <Link href="/signup" className="px-6 py-3 rounded-lg bg-blue-600 text-white hover:bg-blue-700 flex items-center gap-2">Sign Up</Link>
          <Link href="/signin" className="px-6 py-3 rounded-lg bg-white text-blue-600 border hover:bg-blue-50 flex items-center gap-2">Sign In</Link>
          <button
            className="px-6 py-3 rounded-lg bg-white text-blue-600 border hover:bg-blue-50 flex items-center gap-2"
            onClick={() => router.push('/explore')}>
            Browse as Guest
          </button>
          <Link href="/explore" className="px-6 py-3 rounded-lg bg-blue-50 text-blue-600 hover:bg-blue-100 flex items-center gap-2">
            Explore Puppies
          </Link>
        </div>

        {/* Feature bullets */}
        <ul className="mt-8 flex flex-wrap gap-6 justify-center text-sm text-gray-500">
          <li className="flex items-center gap-1">🟢 Verified Breeders</li>
          <li className="flex items-center gap-1">❤️ Health Guaranteed</li>
          <li className="flex items-center gap-1">⭐ 5-Star Support</li>
        </ul>
      </section>
    </main>
  );
}
//------------------------------------------------

2.  Remove duplicated header/navigation rows that the previous agent injected:
    • Delete any extra <header> or “Back to Browse” markup in greeting.jsx.

3.  Ensure white text on blue buttons:
    • Use `text-white` for Sign Up / Browse as Guest buttons when bg is blue.
    • Keep Sign In / Explore Puppies buttons blue text on white background.

4.  Auth redirects (verify but don’t change logic):
    • /       → /greeting if !session
    • /profile → /greeting if !session
    • /messages → /greeting if !session

5.  Do **NOT** alter ExplorePage logic added earlier (demo vs. live split).

// ───────── END PROMPT ─────────

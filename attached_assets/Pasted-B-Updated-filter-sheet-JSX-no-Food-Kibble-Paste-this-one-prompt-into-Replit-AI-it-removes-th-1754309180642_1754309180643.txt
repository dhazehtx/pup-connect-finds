B. Updated filter-sheet JSX (no “Food & Kibble”)
Paste this one prompt into Replit-AI; it removes the food category, renames the group to just “Treats,” and keeps everything else unchanged.

bash
Copy
Edit
### TASK: update Store filter sheet – drop kibble, keep Treats only

SCOPE
• client/src/components/Store/FilterSheet.tsx  (or equivalent)
• NO other files touched.

STEPS
1.  Replace the current “Categories” block with:

```tsx
{/* Categories */}
<h3 className="text-lg font-semibold mb-2">Categories</h3>
<div className="flex flex-col gap-3">
  <Checkbox
    checked={filters.treats}
    onCheckedChange={v => setFilters(f => ({ ...f, treats: v }))}
    id="treats"
    label="Treats"
  />
  <Checkbox
    checked={filters.accessories}
    onCheckedChange={v => setFilters(f => ({ ...f, accessories: v }))}
    id="accessories"
    label="Accessories"
  />
  <Checkbox
    checked={filters.toys}
    onCheckedChange={v => setFilters(f => ({ ...f, toys: v }))}
    id="toys"
    label="Toys"
  />
</div>
Remove any “Food & Treats” references in:
• filterOptions.ts
• productSeeds.ts (update sample data so nothing has category food)

Types

ts
Copy
Edit
type Category = 'treats' | 'accessories' | 'toys';
Filtering logic

ts
Copy
Edit
filtered = products.filter(p =>
  (!filters.treats || p.category==='treats') &&
  (!filters.accessories || p.category==='accessories') &&
  (!filters.toys || p.category==='toys')
);
Commit

bash
Copy
Edit
git commit -am "feat(filters): remove kibble category, keep Treats only"
C. Next steps
Compliance assets
– Draft an AAFCO-compliant label template now.
– Create a per-state registration spreadsheet before launch.

When the UI is locked, we can add stripe_price_id to each treat product and generate a checkout session in /api/create-checkout-session. 1 ▸ Filters worth adding for a pet-supply store
Group	Why it helps shoppers	UI control
Price – min / max slider (you already have)	Classic budget filter	Dual-thumb slider
Rating – minimum stars	Surfaces reputable items quickly	Star-slider or “4★ & up” chips
Availability
• In-stock only
• Ships today	Avoids out-of-stock frustration	Checkbox pair
Size / Breed weight class	Beds, collars, food portions vary by dog size	Pill chips: XS • S • M • L • XL
Age group – Puppy / Adult / Senior	Nutritional & toy needs differ	3-way segmented control
Product type
• Toys (chew, puzzle…treats…)
• Gear (leash, crate…)	Lets owners jump to what they need	Nested accordion, one sub-checkbox list each
Brand	Brand-loyal customers	Scrollable checkbox list
Eco / Organic	Growing demand for sustainable goods	Single checkbox
Subscription eligible	Surfaces items that can recur (great for food)	Toggle

—
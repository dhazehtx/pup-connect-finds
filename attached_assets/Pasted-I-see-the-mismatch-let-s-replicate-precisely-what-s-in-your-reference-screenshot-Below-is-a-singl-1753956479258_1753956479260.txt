I see the mismatch — let’s replicate precisely what’s in your reference screenshot.
Below is a single Replit-AI prompt that:

Removes the duplicate logo row

Keeps the search bar in the white header exactly as-shown

Puts the four pill buttons in one tight row, all the same blue, all with white text (no outlines)

Uses the exact widths I can infer from the shot (≈ 128 px for the first three, ≈ 190 px for “Explore Puppies”)

Keeps the feature bullets & bottom nav visible

Adds a light 1 px divider under the header (I can see it faintly in the image)

Copy–paste everything below into the Replit AI console, approve the diff, then hit Run.

javascript
Copy
Edit
/* ───────────────────────────────────────────────────────
   GREETING PAGE  – PIXEL-PERFECT TO MATCH SCREENSHOT
──────────────────────────────────────────────────────── */

1 – Replace the ENTIRE file `src/pages/greeting.jsx`:

import Link from 'next/link';
import { useRouter } from 'next/router';

export default function Greeting() {
  const router = useRouter();

  return (
    <main className="min-h-screen flex flex-col bg-gradient-to-br from-blue-50 to-blue-100">
      {/* top header – white bar with logo, search, auth buttons */}
      <header className="flex items-center gap-6 px-8 py-3 bg-white shadow-sm border-b">
        {/* logo */}
        <span className="flex items-center gap-2 text-xl font-bold whitespace-nowrap">
          💙 MY PUP
        </span>

        {/* search bar */}
        <input
          type="text"
          placeholder="Search puppies, breeds, or breeders…"
          className="flex-1 max-w-lg px-4 py-2 bg-gray-100 rounded-md text-sm focus:outline-none"
        />

        {/* mini auth buttons */}
        <div className="flex gap-3">
          <Link href="/signin"
            className="px-4 py-1 rounded-full bg-blue-600 text-white text-sm hover:bg-blue-700">
            Sign In
          </Link>
          <Link href="/signup"
            className="px-4 py-1 rounded-full bg-blue-600 text-white text-sm hover:bg-blue-700">
            Sign Up
          </Link>
        </div>
      </header>

      {/* hero */}
      <section className="flex flex-col items-center justify-center flex-1 text-center px-4">
        <h1 className="text-4xl md:text-5xl font-extrabold leading-tight">
          Find Your Perfect <br />
          <span className="text-blue-600">Puppy Companion</span>
        </h1>

        <p className="mt-4 max-w-xl text-gray-600">
          Connect with verified breeders and discover adorable, healthy puppies
          waiting for their forever homes.
        </p>

        {/* pill buttons row */}
        <div className="mt-10 flex flex-wrap gap-4 justify-center">
          <Link href="/signup"
            className="w-[128px] px-6 py-3 rounded-full bg-blue-600 text-white hover:bg-blue-700 shadow">
            Sign Up
          </Link>
          <Link href="/signin"
            className="w-[128px] px-6 py-3 rounded-full bg-blue-600 text-white hover:bg-blue-700 shadow">
            Sign In
          </Link>
          <button
            onClick={() => router.push('/explore')}
            className="w-[128px] px-6 py-3 rounded-full bg-blue-600 text-white hover:bg-blue-700 shadow">
            Browse as Guest
          </button>
          <Link href="/explore"
            className="w-[190px] px-6 py-3 rounded-full bg-blue-600 text-white hover:bg-blue-700 shadow">
            Explore Puppies
          </Link>
        </div>

        {/* feature bullets */}
        <ul className="mt-8 flex flex-wrap gap-6 justify-center text-sm text-gray-500">
          <li className="flex items-center gap-1">🟢 Verified Breeders</li>
          <li className="flex items-center gap-1">❤️ Health Guaranteed</li>
          <li className="flex items-center gap-1">⭐ 5-Star Support</li>
        </ul>
      </section>
    </main>
  );
}

2 – **Delete** any leftover duplicate `<header>` or stray markup the previous agent inserted (search the file for a second `<header>` tag – there should be only one after this patch).

3 – Buttons are now *all* `bg-blue-600 text-white`; no outlined variants.

4 – Keep bottom nav visible (no hide-logic).

5 – Auth guards remain unchanged:
    • `/`, `/profile`, `/messages` → redirect to `/greeting` when `!session`.

/* ───────────────────────────────────────────── */
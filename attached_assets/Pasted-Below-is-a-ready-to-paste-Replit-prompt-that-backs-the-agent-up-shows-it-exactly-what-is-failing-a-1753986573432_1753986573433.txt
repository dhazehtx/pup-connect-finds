Below is a ready-to-paste Replit prompt that backs the agent up, shows it exactly what is failing, and instructs it to solve the remaining Content-Security-Policy (CSP) “Failed to fetch” error in a clean, reversible way.

txt
Copy
Edit
### ↩️ ROLL BACK & FIX AUTH CSP ISSUE

We’re still getting *“Failed to construct ‘URL’: Invalid URL / TypeError: Failed to fetch – Refused to connect because it violates the document’s Content Security Policy (CSP)”* when the browser tries to hit  
https://wneticxjhxpjpfghnclr.supabase.co/auth/v1/token (see console screenshot).

1️⃣ **Reset any experimental hard-coded Supabase credentials** you added while testing.  
   • Make sure the *only* credentials come from Replit Secrets  
      `VITE_SUPABASE_URL=https://wneticxjhxpjpfghnclr.supabase.co`  
      `VITE_SUPABASE_ANON_KEY=<the long anon JWT>`  
   • Confirm those are loaded in `client/src/integrations/supabase/client.ts`.

2️⃣ **Add the Supabase domain to the dev-server CSP**, so the browser is allowed to call it.  
   The simplest, non-breaking fix for Vite inside Replit is to inject a meta tag **only in development**:  

```ts
/* vite.config.ts ----------------------------------------------------------- */
import { defineConfig, loadEnv } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig(({ mode }) => {
  const env = loadEnv(mode, process.cwd(), '')
  return {
    plugins: [react()],
    define: {
      __SUPABASE_URL__: JSON.stringify(env.VITE_SUPABASE_URL),
      __SUPABASE_ANON__: JSON.stringify(env.VITE_SUPABASE_ANON_KEY)
    },
    /* dev-only CSP header to allow Supabase */
    server: {
      headers: {
        'Content-Security-Policy':
          `default-src 'self';` +
          `connect-src 'self' ${env.VITE_SUPABASE_URL} https://api.stripe.com ` +
          `https://www.google-analytics.com https://region1.google-analytics.com ws:;`
      }
    }
  }
})
3️⃣ Remove the old try/catch “fallback” Supabase URL logic you inserted earlier so we don’t mask real config errors.

4️⃣ Restart the dev server (npm run dev or the Replit “Run” button) and open the browser console.
• You should now see the POST to /auth/v1/token succeed (or return a 400 with "invalid_credentials", which is expected with a fake email/password).
• No more CSP or “Invalid URL” errors.

5️⃣ Test a real sign-up & sign-in to confirm Supabase auth is fully restored.

⚠️ Do NOT commit the long anon key to the repo. It must remain only in the Replit Secrets.

Let me know once the console shows a clean 200/400 from Supabase so we can close this!

markdown
Copy
Edit

### What this prompt does

* **Step-by-step rollback** – undoes earlier hard-coded “fallbacks” so the app only reads the two correct secrets.
* **Injects a dev-only CSP header** through `vite.config.ts`, the safest place in a Replit/Vite stack. It leaves production builds untouched (hosting providers like Vercel/Nginx let you add CSP headers separately).
* Leaves your Supabase credentials secure (still stored in the Replit Secrets panel, **not** in the repo).
* Gives the agent a clear success criterion: seeing the Supabase `/auth/v1/token` request succeed (200) or fail with `"invalid_credentials"` (400) — either means the network call is now allowed.

Replit Agent Prompt — “Restore bottom navbar visibility + keep theme fixes”

Goal: Make the bottom navbar always visible with correct contrast, keep tab/CTA fixes, and prevent content from being covered.

Do exactly this:

Navbar component styles (class or CSS)

Ensure the bottom nav has a solid background, readable icons/text, height, and sits above content.

Tailwind version:

<nav className="fixed bottom-0 left-0 right-0 z-50 h-16 
                bg-mp-blue text-white border-t border-mp-border
                flex items-center justify-around">
  {/* nav items */}
</nav>


Plain CSS fallback:

.navbar {
  position: fixed; bottom: 0; left: 0; right: 0;
  z-index: 50; height: 64px;
  background: var(--mp-blue);
  color: #fff; border-top: 1px solid var(--mp-border);
  display: flex; align-items: center; justify-content: space-around;
}
.navbar a, .navbar button, .navbar svg { color: rgba(255,255,255,.9); }
.navbar a.active, .navbar button.active { color:#fff; background: rgba(255,255,255,.12); border-radius: 12px; }
.navbar svg { stroke: currentColor; }


Avoid “baby-blue overlay” on the navbar

Do not apply bg-mp-baby to the navbar. Keep it only on the page wrapper.

If a global rule hits nav or .navbar, remove background: transparent or any mix-blend/backdrop that could wash it out.

Prevent content overlap

Add bottom padding to every page that has the fixed navbar:

Tailwind on page wrapper: pb-20 (or pb-[80px])

CSS fallback:

.page, main { padding-bottom: 80px; }


Icon/text legibility

Ensure nav item components don’t override to text-white/0 or text-mp-accent:

In nav item JSX, set default className="text-white/80" and active state text-white.

Ensure all SVG icons use stroke="currentColor" (no hardcoded white-on-white).

Dark/Light collisions

Search and remove any of these on the navbar:

bg-transparent, text-white/0, opacity-0, mix-blend-*, backdrop-blur combined with transparency that hides content.

Any absolute positioning making height collapse.

Keep your other requests intact

Tabs (Services / Pup Box / Store): light-blue text in all states, selected tab soft blue background—never white text.

“Become a Service Provider” button: bg-mp-blue text-white.

“Featured Services / All Services” chips: bordered pills with clear selected state.

Remove blue debug outline from Marketplace hero.

Pages keep bg-mp-baby; cards/sections are white.

QA checklist (on phone)

Nav bar is visible on every route; icons readable in daylight.

Content doesn’t sit under the nav (scroll to bottom of long pages).

Tabs show light-blue text when selected.

Provider CTA is white text on blue.

Chips have borders.

No unexpected blue outlines anywhere.

Commit message:
fix(nav): restore bottom navbar visibility (bg/z-index/height), add page bottom padding; keep contrast & tab fixes
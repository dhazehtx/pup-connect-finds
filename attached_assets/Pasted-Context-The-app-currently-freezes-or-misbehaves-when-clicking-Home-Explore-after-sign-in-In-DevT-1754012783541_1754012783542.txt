Context:
The app currently freezes or misbehaves when clicking Home / Explore after sign-in. In DevTools the error shows:
  TypeError: Failed to execute 'fetch' on 'Window': '/api/posts/home-feed' is not a valid HTTP method.
That tells me the fetch usage is wrong (the path is being treated as a method or the arguments are swapped), and navigation guard logic may be causing redundant redirect logic or silent hangs. Fix the broken Home/HomeFeed and Explore navigation, stabilize auth-dependent rendering, and ensure fallback/loading state so UI doesn't freeze when data is delayed or errors occur.

Tasks to implement:

1. **Fix the shared API helper (`client/src/lib/api.ts`)** so all requests use correct signature and headers, support timeouts, and surface errors cleanly. Replace any broken helper with something like:

```ts
import { supabase } from '@/integrations/supabase/client';

const BACKEND_PREFIX = '/api'; // adjust if your proxy path differs

export async function apiRequest(
  path: string,
  {
    method = 'GET',
    headers = {},
    signal,
    body,
    ...rest
  }: RequestInit & { body?: any } = {}
) {
  const {
    data: { session },
  } = await supabase.auth.getSession();
  const token = session?.access_token || '';

  const url = path.startsWith('/') ? path : `/${path}`;
  const fetchUrl = `${BACKEND_PREFIX}${url}`; // e.g. /api/posts/home-feed

  // timeout fallback
  const controller = new AbortController();
  const finalSignal = signal || controller.signal;
  const timeout = setTimeout(() => controller.abort(), 5000);

  try {
    const res = await fetch(fetchUrl, {
      method,
      headers: {
        Accept: 'application/json',
        Authorization: token ? `Bearer ${token}` : '',
        ...(body ? { 'Content-Type': 'application/json' } : {}),
        ...headers,
      },
      body: body ? JSON.stringify(body) : undefined,
      signal: finalSignal,
      ...rest,
    });
    clearTimeout(timeout);
    if (!res.ok) {
      const text = await res.text();
      throw new Error(`API request failed ${res.status}: ${text}`);
    }
    return await res.json();
  } catch (e) {
    clearTimeout(timeout);
    console.error('[apiRequest] error for', path, e);
    throw e;
  }
}

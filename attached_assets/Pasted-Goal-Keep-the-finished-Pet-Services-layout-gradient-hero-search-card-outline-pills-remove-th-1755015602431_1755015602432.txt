Goal: Keep the finished Pet Services layout (gradient hero + search card + outline pills), remove the older “Find trusted…” hero/filters, and ensure there’s only one search + one pill row.

Open client/src/pages/Marketplace/index.tsx (or ServicesTab.tsx, whichever renders the Services page).

Search for:

"Find trusted pet service providers" (old v1 hero)

"Pet Services Marketplace" (new v2 hero)

Delete the entire block containing the old v1 hero and its inline CTA + filters.

Ensure the Services content only renders when the Services tab is active:

tsx
Copy
Edit
if (activeTab !== "services") return null;
Keep exactly one search card and one filter pill row directly under the gradient hero.

Replace any leftover pill markup with the unified FilterPill component:

tsx
Copy
Edit
import { FilterPill } from "@/components/FilterPill";
<div className="mt-4 flex flex-wrap gap-3">
  <FilterPill label="All Services" selected={filter==="all"} onClick={()=>setFilter("all")} />
  <FilterPill label="Grooming"     selected={filter==="grooming"} onClick={()=>setFilter("grooming")} />
  <FilterPill label="Dog Sitting"  selected={filter==="sitting"} onClick={()=>setFilter("sitting")} />
  <FilterPill label="Training"     selected={filter==="training"} onClick={()=>setFilter("training")} />
  <FilterPill label="Dog Walking"  selected={filter==="walking"} onClick={()=>setFilter("walking")} />
  <FilterPill label="Boarding"     selected={filter==="boarding"} onClick={()=>setFilter("boarding")} />
</div>
(Unselected = bg-blue-50 text-blue-700 border-blue-500; Selected = bg-blue-600 text-white border-blue-600 — already implemented in FilterPill.)

Keep one CTA:

Either the gradient hero’s inline CTA or the bottom “Get Started” promo—but not both.

If you keep the bottom promo, ensure it’s a single section near the footer and not a second hero.

Remove any leftover classes that could fight the style (opacity-*, text-slate-* on pills/buttons). Pills should only use the FilterPill styles.

QA:

Scroll the page: you should see one hero, one search card, one pill row.

Unselected pill: light-blue bg + dark-blue text and border.

Selected pill: solid blue bg + white text.

“Book Service” buttons: blue bg + white text.

No duplicate sections at the bottom.

Hard refresh (Cmd+Shift+R).
Step 2 – Use these classes in your JSX

Where you render the three tabs (Pet Services / Pup Box / Store), adjust to something like:

// example – adjust to your actual state names
const [activeTab, setActiveTab] = useState('pupbox');

<div className="marketplace-tabs">
  <button
    className={`marketplace-tab ${activeTab === 'services' ? 'marketplace-tab--active' : ''}`}
    onClick={() => setActiveTab('services')}
  >
    Pet Services
  </button>

  <button
    className={`marketplace-tab ${activeTab === 'pupbox' ? 'marketplace-tab--active' : ''}`}
    onClick={() => setActiveTab('pupbox')}
  >
    Pup Box
  </button>

  <button
    className={`marketplace-tab ${activeTab === 'store' ? 'marketplace-tab--active' : ''}`}
    onClick={() => setActiveTab('store')}
  >
    Store
  </button>
</div>


You probably already have state for which tab is selected — just plug these classes into that.

4. Plain CSS for product cards (store + “Add / Buy Now” buttons)

This will get you close to those last three screenshots with the big, clean cards.

Step 1 – CSS for cards & buttons

Add this below the previous CSS:

/* Product card */
.product-card {
  background-color: #ffffff;
  border-radius: 12px;
  box-shadow: 0 12px 30px rgba(15, 23, 42, 0.06);
  overflow: hidden;
  display: flex;
  flex-direction: column;
  height: 100%;
}

/* Image area – fixed height so all cards line up */
.product-card__image {
  height: 260px;          /* adjust if needed */
  background-color: #f9fafb;
  overflow: hidden;
}

.product-card__image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/* Card body */
.product-card__body {
  padding: 16px;
  display: flex;
  flex-direction: column;
  flex: 1;
}

.product-card__title {
  font-size: 1rem;
  font-weight: 600;
  color: #111827;
  margin-bottom: 4px;
}

.product-card__description {
  font-size: 0.875rem;
  color: #6b7280;
  margin-bottom: 12px;
  flex: 1;                 /* pushes buttons to bottom */
}

/* Buttons row */
.product-card__actions {
  display: flex;
  gap: 12px;
  margin-top: auto;
}

/* Buttons – pill style */
.btn-pill {
  flex: 1;
  padding: 8px 0;
  border-radius: 9999px;
  font-weight: 600;
  font-size: 0.9rem;
  cursor: pointer;
  transition:
    background-color 0.18s ease,
    color 0.18s ease,
    box-shadow 0.18s ease;
}

/* Secondary (Add) */
.btn-pill--outline {
  border: 1px solid #1f6feb;
  background-color: #ffffff;
  color: #1f6feb;
}

.btn-pill--outline:hover {
  background-color: rgba(31, 111, 235, 0.08);
}

/* Primary (Buy Now / Checkout) */
.btn-pill--primary {
  border: none;
  background-color: #1f6feb;
  color: #ffffff;
  box-shadow: 0 8px 18px rgba(31, 111, 235, 0.35);
}

.btn-pill--primary:hover {
  background-color: #1557c3;
}

Step 2 – Wrap your product markup with these classes

Where you map over products now, structure it like:

<div className="product-card">
  <div className="product-card__image">
    <img src={product.imageUrl} alt={product.name} />
  </div>

  <div className="product-card__body">
    <h3 className="product-card__title">{product.name}</h3>
    <p className="product-card__description">{product.description}</p>

    <div className="product-card__actions">
      <button
        className="btn-pill btn-pill--outline"
        onClick={() => addToCart(product.id)}
      >
        Add
      </button>
      <button
        className="btn-pill btn-pill--primary"
        onClick={() => buyNow(product.id)}
      >
        Buy Now
      </button>
    </div>
  </div>
</div>
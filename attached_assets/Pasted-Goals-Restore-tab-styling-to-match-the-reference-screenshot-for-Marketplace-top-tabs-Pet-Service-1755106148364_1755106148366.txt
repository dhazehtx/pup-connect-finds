Goals

Restore tab styling to match the reference screenshot for:

Marketplace top tabs: Pet Services | Pup Box | Store

Pet Services sub-tabs: Featured Services | All Services

Keep pill category chips below (Grooming, Dog Sitting, etc.) as-is.

Remove all demo provider listings (e.g., “Austin Reyes”) from Pet Services. When there’s no real data, show the single empty state with CTA (e.g., “Become a Service Provider”).

1) Tab styling (match reference screenshot exactly)
Create a shared SegmentedTab style and apply to:

Top tabs: Pet Services / Pup Box / Store

Sub-tabs: Featured Services / All Services

Design rules

Active: blue background, white text, no hard brown; subtle shadow.

Inactive: white background, blue border and blue text; visible outline.

Rounded full (pill), tight vertical height, even horizontal padding.

Hover: slight blue-tint background on inactive.

Spacing: small gap (gap-2) between tabs.

Tailwind class recipe

tsx
Copy
Edit
import clsx from "clsx";

const SegTab = ({ active, children, ...props }) => (
  <button
    {...props}
    className={clsx(
      "px-4 h-9 rounded-full border text-sm font-medium transition",
      active
        ? "bg-primary text-white border-primary shadow"
        : "bg-white text-primary border-primary hover:bg-primary/10"
    )}
  >
    {children}
  </button>
);
Apply it

Replace existing tab buttons for:

Top bar (Pet Services / Pup Box / Store)

Sub-tabs (Featured Services / All Services)

Remove any conflicting classes like text-brown, bg-white text-white, inline hex colors, or legacy pill/button components.

Ensure the active state is driven only by the current route or selected tab key.

Spacing & container

Keep tabs in a flex row with gap-2 flex-wrap as needed.

Ensure the page background stays light (bg-background or bg-white), not blue.

Acceptance

All four tab groups match the screenshot look/feel (active = blue/white, inactive = white/blue with border).

No odd blue blocks behind sections.

2) Remove demo provider listings & enforce empty state
Data source

Pet Services must render only from real Supabase data (no mocks).

Query must filter to active providers (if such column exists) and must not fall back to demo data when zero/failed:

ts
Copy
Edit
const { data: providers, error } = await supabase
  .from("pet_service_providers") // confirm table name
  .select("*")
  .eq("is_active", true); // include if this column exists

// Never render demo data
const hasLive = Array.isArray(providers) && providers.length > 0;
Render logic

tsx
Copy
Edit
return (
  <>
    <HeaderWithSearchAndAdvancedFilters />
    {hasLive ? (
      <ProvidersGrid providers={providers} />
    ) : (
      <EmptyState
        title="No services found"
        subtitle="Be the first to list a service!"
        ctaLabel="Become a Service Provider"
        onCtaClick={() => router.push("/marketplace/providers/new")} // or the correct route
      />
    )}
  </>
);
Remove legacy/demo

Delete or disable any imports/usages of demoProviders, AustinReyes*, DemoServiceCards, FeaturedServiceDemo, etc.

Ensure guest and signed-in paths both use the same renderer above.

Remove any second/duplicate section that might still render below.

Acceptance

With no real providers, Pet Services shows only the single empty state with CTA (no “Austin Reyes” anywhere).

When providers exist, only those render.


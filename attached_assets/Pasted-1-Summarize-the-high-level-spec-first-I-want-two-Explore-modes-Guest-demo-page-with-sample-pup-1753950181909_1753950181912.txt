1. Summarize the high-level spec first
“I want two Explore modes:
• Guest: demo page with sample puppies. Any click → /greeting.
• Signed-in: advanced filters (breed, gender, price, location, age) + live Supabase results (including my own posts).”

That single sentence tells the agent the business rule before you dive into code.

2. List the concrete tasks in numbered bullets
css
Copy
Edit
1. Restore AdvancedFilters + PuppyGrid for signed-in users (connected to Supabase).
2. Keep DemoExplore (sample puppies) for guests.
3. Add click guard in PostCard / PuppyGrid:
     if not supabase.auth.session() → router.push('/greeting')
4. Leave Marketplace always public.
Numbering helps the agent tackle each change without wandering.

3. Give or paste the exact code snippets (optional but powerful)
Paste the two code blocks I provided earlier under separate headings (// src/pages/explore.jsx, // src/components/PostCard.jsx). Replit’s agent will patch those files exactly.

4. Finish with testing instructions
“After the patch, automatically run these sanity checks:
• Signed-out /explore shows DemoExplore and clicking a card redirects to /greeting.
• Signed-in /explore shows filters and returns live data.
• Signed-out / or /profile still redirects to /greeting.”

This tells the agent to verify the result (and you’ll see its screenshots / logs).

5. Commit or snapshot first
Before running any big prompt, hit Version Control → Commit so you can roll back if the agent misinterprets something.

Example “one-paste” prompt you can copy
csharp
Copy
Edit
// High-level spec
I need two distinct Explore modes:
• Guest (no session): show DemoExplore with sample puppies. Clicking any listing redirects to /greeting.
• Signed-in: show advanced filters (breed, gender, price, location, age) + live Supabase puppy grid. Own posts should appear.

// Tasks
1. Update src/pages/explore.jsx to render DemoExplore when supabase.auth.session() == null, otherwise render AdvancedFilters and PuppyGrid pulled from Supabase (filters in query).
2. Ensure PostCard and PuppyGrid items push /greeting if no session, else /post/[id].
3. Leave Marketplace public; existing auth redirects on Home/Profile stay unchanged.

// Testing
After patch:
• Signed-out → /explore shows demo; clicking any card → /greeting.
• Signed-in → /explore shows filters + live grid.
• Signed-out → / and /profile redirect to /greeting.
Paste that into the Replit AI console, review the diff, and run. N
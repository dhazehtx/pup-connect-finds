
import React from 'react';
import AppCompletionDashboard from '@/components/admin/AppCompletionDashboard';

const AppCompletion = () => {
  return <AppCompletionDashboard />;
};

export default AppCompletion;
